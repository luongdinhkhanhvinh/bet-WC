{{#> layout}}
{{#*inline "head"}}
<title>Register - {{@websiteName}}</title>
{{/inline}}
{{#*inline "body"}}
{{> navbar}}
<div class="container form-signin"
style="
    display: flex;
    align-content: center;
    height: 100vh;
    width: 450px;
">

    <div style="
    margin-top: auto;
    margin-bottom: auto;
    width: 450px;
">  
<h2 class="form-signin-heading text-color-white">Tài khoản mới</h2>
    <form action="/register" method="POST">
        {{#if error}}
        <div class="alert alert-danger" role="alert">
            <strong class="text-color-white">Đăng ký không thành công.</strong> {{error}}
        </div>
        {{/if}}
        <input type="hidden" name="_csrf" value="{{csrfToken}}">
        <div class="form-group">
            <label for="name" class="text-color-white">Tên</label>
            <input type="text" class="form-control" name="name" id="name" placeholder="Anonymous" minlength="3" maxlength="30" required value="{{name}}">
            <small class="form-text text-muted">
               Độ dài tối đa là 30 ký tự. Tên của bạn sẽ được hiển thị cho bất cứ ai. Bạn có thể thay đổi tên của bạn bất cứ lúc nào.
            </small>
        </div>
        <div class="form-group">
            <label for="email" class="text-color-white">Địa chỉ email</label>
            <input type="email" class="form-control" name="email" id="email" placeholder="info@example.com" required value="{{email}}">
            <small class="form-text text-muted">Chúng tôi sẽ gửi một liên kết xác nhận đến tài khoản email của bạn.</small>
        </div>
        <div class="form-group">
            <label for="password" class="text-color-white">Mật khẩu</label>
            <input type="password" class="form-control" name="password" id="password" placeholder="Mật khẩu của bạn" minlength="6" required>
            <small class="form-text text-muted">Chúng tôi mã hóa tất cả mật khẩu bằng bcrypt, một trong những thuật toán băm mật khẩu tốt nhất hiện có.</small>
        </div>
        <button class="btn btn-primary btn-block" type="submit" 
        style="
    background: linear-gradient(to right,#ba33db 0%,#ed2b7b 100%);
    border-radius: 20px;
    border: 1px solid transparent;
"
        >ĐĂNG KÝ</button>
    </form>
    </div>
    
    
</div>
{{/inline}}
{{/layout}}
